export class Config {
    // ----- API_URL ----------------------------------------------------------
    // url prema api-ima
    public API_URL = 'https://localhost:5001/api/';

    // ----- APIRetryCount ----------------------------------------------------
    // definira koliko puta ce se pozvati api ako pukne
    public APIRetryCount = 3;
}
